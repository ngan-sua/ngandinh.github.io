[{{- range $index, $page := where .Site.RegularPages "Section" "posts" -}}{{- if $index -}},{{- end -}}{"title": {{ $page.Title | jsonify }},"url": {{ $page.RelPermalink | jsonify }},"date": {{ $page.Date.Format "02/01/2006" | jsonify }},"summary": {{ $page.Summary | plainify | jsonify }},"content": {{ $page.Plain | jsonify }},"categories": [{{- range $cindex, $category := $page.Params.categories -}}{{- if $cindex -}},{{- end -}}{{ $category | jsonify }}{{- end -}}]}{{- end -}}]
